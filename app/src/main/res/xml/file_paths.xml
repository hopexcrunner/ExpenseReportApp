<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App-specific external files directory (doesn't require permissions) -->
    <external-files-path 
        name="external_files" 
        path="." />
    <!-- Internal files directory -->
    <files-path 
        name="files" 
        path="." />
    <!-- Cache directory -->
    <cache-path 
        name="cache" 
        path="." />
</paths>
